?str
summary(x)
str(x)
x[,1]>="12/31/2013"
as.Date(x[,1])>="12/31/2013"
x[1,1]
str(date)
str(as.Date)
summary(PriusMPG)
summary(x)
factor
?factor
colMeans(x[,7])
str(colMeans)
?colMeans
class(x[,7])
x[,7]
colMeans(x[,7],na.rm=TRUE)
x
set.seed(1)
rpois(5, 2)
set.seed(10)
x <- rbinom(10, 10, 0.5)
e <- rnorm(10, 0, 20)
y <- 0.5 + 2 * x + e
install.packages("~/Downloads/xlsx_0.5.5.tgz", repos = NULL)
library(xlsx)
R.Version()
R.Version()
install.packages("xlsx")
library(xlsx)
rowIndex <- 18:23
colIndex <- 7:15
dat <- read.xlsx("./data/NGAP.xlsx,sheetIndex=1,colIndex=colIndex,rowIndex=rowIndex")
install.packages("devtools")
?read.csv
setwd("~/CourseraHW/RepData_PeerAssessment1")
# Reproducible Research: Peer Assessment 1
## Loading and preprocessing the data
Set working directory:
``` {r}
setwd("~/CourseraHW/RepData_PeerAssessment1")
```
Create a data subdirectory if it does not exist:
``` {r}
if (!file.exists("data")) {
dir.create("data")
}
```
Download file and unzip if it isn't already there:
``` {r}
filePath <- "./data/"
fileZipName <- "repdata-data-activity.zip"
fileName <- "activity.csv"
fileUrl <- "https://d396qusza40orc.cloudfront.net/repdata%2Fdata%2Factivity.zip"
ZipFile <- paste0(filePath, fileZipName)        # path to zipped data file
if (!file.exists(ZipFile)) {
download.file(fileUrl, destfile = ZipFile, method ="curl")
unzip(ZipFile, exdir=filePath)
dateDownloaded <- date()
}
```
Read in activity monitoring dataset:
``` {r}
amd <- read.csv(paste0(filePath, fileName))
```
## What is mean total number of steps taken per day?
## What is the average daily activity pattern?
## Imputing missing values
## Are there differences in activity patterns between weekdays and weekends?
View(amd)
View(amd)
?echo
tail(amd)
# Reproducible Research: Peer Assessment 1
## Loading and preprocessing the data
Set working directory:
``` {r}
echo = FALSE
setwd("~/CourseraHW/RepData_PeerAssessment1")
```
Create a data subdirectory if it does not exist:
``` {r}
if (!file.exists("data")) {
dir.create("data")
}
```
Download file and unzip if it isn't already there:
``` {r}
filePath <- "./data/"
fileZipName <- "repdata-data-activity.zip"
fileName <- "activity.csv"
fileUrl <- "https://d396qusza40orc.cloudfront.net/repdata%2Fdata%2Factivity.zip"
ZipFile <- paste0(filePath, fileZipName)        # path to zipped data file
if (!file.exists(ZipFile)) {
download.file(fileUrl, destfile = ZipFile, method ="curl")
unzip(ZipFile, exdir=filePath)
dateDownloaded <- date()
}
```
Read in activity monitoring dataset:
``` {r}
mydat <- read.csv(paste0(filePath, fileName))
```
## What is mean total number of steps taken per day?
For this part of the assignment, you can ignore the missing values in the dataset.
Make a histogram of the total number of steps taken each day.
For each data point, create a column for the day it occurred. Each interval represents a 5-minute total, therefore there are 12 data points per hour and 288 data points per day.
```{r}
```
Calculate and report the mean and median total number of steps taken per day
## What is the average daily activity pattern?
## Imputing missing values
## Are there differences in activity patterns between weekdays and weekends?
# Reproducible Research: Peer Assessment 1
## Loading and preprocessing the data
Set working directory:
``` {r}
echo = FALSE
setwd("~/CourseraHW/RepData_PeerAssessment1")
```
Create a data subdirectory if it does not exist:
``` {r}
if (!file.exists("data")) {
dir.create("data")
}
```
Download file and unzip if it isn't already there:
``` {r}
filePath <- "./data/"
fileZipName <- "repdata-data-activity.zip"
fileName <- "activity.csv"
fileUrl <- "https://d396qusza40orc.cloudfront.net/repdata%2Fdata%2Factivity.zip"
ZipFile <- paste0(filePath, fileZipName)        # path to zipped data file
if (!file.exists(ZipFile)) {
download.file(fileUrl, destfile = ZipFile, method ="curl")
unzip(ZipFile, exdir=filePath)
dateDownloaded <- date()
}
```
Read in activity monitoring dataset:
``` {r}
mydat <- read.csv(paste0(filePath, fileName))
```
## What is mean total number of steps taken per day?
For this part of the assignment, you can ignore the missing values in the dataset.
Make a histogram of the total number of steps taken each day.
For each data point, create a column for the day it occurred. Each interval represents a 5-minute total, therefore there are 12 data points per hour and 288 data points per day.
```{r}
```
Calculate and report the mean and median total number of steps taken per day
## What is the average daily activity pattern?
## Imputing missing values
## Are there differences in activity patterns between weekdays and weekends?
View(mydat)
View(mydat)
tail(mydat)
str(amd)
str(mydat)
avg(steps)
?sum
x <- aggregate(mydata by date)
aggregate(steps, data = mydat, mean)
aggregate(mydata$steps, data = mydat, sum)
aggregate(mydat$steps, data = mydat, FUN=sum)
```
aggregate(x = mydat, by = date, FUN = "mean")
lapply(mydat, mean)
?lapply
rowsum(mydat, date, reorder = TRUE, na.rm = FALSE, ...)
rowsum(mydat, date, reorder = TRUE, na.rm = FALSE)
# Reproducible Research: Peer Assessment 1
## Loading and preprocessing the data
Set working directory:
``` {r}
setwd("~/CourseraHW/RepData_PeerAssessment1")
```
Create a data subdirectory if it does not exist:
``` {r}
if (!file.exists("data")) {
dir.create("data")
}
```
Download file and unzip if it isn't already there:
``` {r}
filePath <- "./data/"
fileZipName <- "repdata-data-activity.zip"
fileName <- "activity.csv"
fileUrl <- "https://d396qusza40orc.cloudfront.net/repdata%2Fdata%2Factivity.zip"
ZipFile <- paste0(filePath, fileZipName)        # path to zipped data file
if (!file.exists(ZipFile)) {
download.file(fileUrl, destfile = ZipFile, method ="curl")
unzip(ZipFile, exdir=filePath)
dateDownloaded <- date()
}
```
Read in activity monitoring dataset:
``` {r}
mydat <- read.csv(paste0(filePath, fileName))
```
setwd("~/CourseraHW/RepData_PeerAssessment1")
if (!file.exists("data")) {
dir.create("data")
}
filePath <- "./data/"
fileZipName <- "repdata-data-activity.zip"
fileName <- "activity.csv"
fileUrl <- "https://d396qusza40orc.cloudfront.net/repdata%2Fdata%2Factivity.zip"
ZipFile <- paste0(filePath, fileZipName)        # path to zipped data file
if (!file.exists(ZipFile)) {
download.file(fileUrl, destfile = ZipFile, method ="curl")
unzip(ZipFile, exdir=filePath)
dateDownloaded <- date()
}
mydat <- read.csv(paste0(filePath, fileName))
echo = TRUE
s <- split(mydat, mydat$Species)
s <- split(mydat, mydat$date)
View(mydat)
steps <-with(mydat, tapply(steps, date, sum))
dailysteps <-with(mydat, tapply(steps, date, sum))
str(dailysteps)
?tapply
?read.csv
setwd("~/CourseraHW/RepData_PeerAssessment1")
if (!file.exists("data")) {
dir.create("data")
}
filePath <- "./data/"
fileZipName <- "repdata-data-activity.zip"
fileName <- "activity.csv"
fileUrl <- "https://d396qusza40orc.cloudfront.net/repdata%2Fdata%2Factivity.zip"
ZipFile <- paste0(filePath, fileZipName)        # path to zipped data file
if (!file.exists(ZipFile)) {
download.file(fileUrl, destfile = ZipFile, method ="curl")
unzip(ZipFile, exdir=filePath)
dateDownloaded <- date()
}
raw <- read.csv(paste0(filePath, fileName))
good <- complete.cases(raw)  #logical vector to weed out NAs
is.na(good)
good <- complete.cases(raw$date)  #logical vector to weed out NAs
good <- complete.cases(raw$steps)  #logical vector to weed out NAs
data<-raw[good,][,]          #complete data only
complete.data<-raw[good,][,]          #complete data only
``` {r}
setwd("~/CourseraHW/RepData_PeerAssessment1")
```
Create a data subdirectory if it does not exist:
``` {r}
if (!file.exists("data")) {
dir.create("data")
}
```
Download file and unzip if it isn't already there:
``` {r}
filePath <- "./data/"
fileZipName <- "repdata-data-activity.zip"
fileName <- "activity.csv"
fileUrl <- "https://d396qusza40orc.cloudfront.net/repdata%2Fdata%2Factivity.zip"
ZipFile <- paste0(filePath, fileZipName)        # path to zipped data file
if (!file.exists(ZipFile)) {
download.file(fileUrl, destfile = ZipFile, method ="curl")
unzip(ZipFile, exdir=filePath)
dateDownloaded <- date()
}
```
Read in activity monitoring dataset:
``` {r}
raw <- read.csv(paste0(filePath, fileName))
```
good <- complete.cases(raw$steps)  #logical vector to weed out NAs
complete.data<-raw[good,][,]          #complete data only
View(raw)
View(complete.data)
is.na(complete.data$steps)
summary(is.na(complete.data$steps))
summary(raw)
summary(raw$steps)
summary(complete.data$steps)
gdata <-raw[good,][,]          #complete data only
summary(gdata)
str(gdata)
dailysteps <-with(gdata, tapply(steps, date, sum))
View(gdata)
dailysteps
dailysteps <-as.data.frame(with(gdata, tapply(steps, date, sum)))
View(dailysteps)
View(gdata)
View(complete.data)
View(raw)
View(raw)
View(gdata)
View(gdata)
# Reproducible Research: Peer Assessment 1
## Loading and preprocessing the data
Set working directory:
``` {r}
setwd("~/CourseraHW/RepData_PeerAssessment1")
```
Create a data subdirectory if it does not exist:
``` {r}
if (!file.exists("data")) {
dir.create("data")
}
```
Download file and unzip if it isn't already there:
``` {r}
filePath <- "./data/"
fileZipName <- "repdata-data-activity.zip"
fileName <- "activity.csv"
fileUrl <- "https://d396qusza40orc.cloudfront.net/repdata%2Fdata%2Factivity.zip"
ZipFile <- paste0(filePath, fileZipName)        # path to zipped data file
if (!file.exists(ZipFile)) {
download.file(fileUrl, destfile = ZipFile, method ="curl")
unzip(ZipFile, exdir=filePath)
dateDownloaded <- date()
}
```
Read in activity monitoring dataset:
``` {r}
raw <- read.csv(paste0(filePath, fileName))
```
setwd("~/CourseraHW/RepData_PeerAssessment1")
if (!file.exists("data")) {
dir.create("data")
}
filePath <- "./data/"
fileZipName <- "repdata-data-activity.zip"
fileName <- "activity.csv"
fileUrl <- "https://d396qusza40orc.cloudfront.net/repdata%2Fdata%2Factivity.zip"
ZipFile <- paste0(filePath, fileZipName)        # path to zipped data file
if (!file.exists(ZipFile)) {
download.file(fileUrl, destfile = ZipFile, method ="curl")
unzip(ZipFile, exdir=filePath)
dateDownloaded <- date()
}
raw <- read.csv(paste0(filePath, fileName))
clean <- na.omit(raw)
View(clean)
dailysteps <-as.data.frame(with(clean, tapply(steps, date, sum)))
View(dailysteps)
View(raw)
setwd("~/CourseraHW/RepData_PeerAssessment1")
if (!file.exists("data")) {
dir.create("data")
}
filePath <- "./data/"
fileZipName <- "repdata-data-activity.zip"
fileName <- "activity.csv"
fileUrl <- "https://d396qusza40orc.cloudfront.net/repdata%2Fdata%2Factivity.zip"
ZipFile <- paste0(filePath, fileZipName)        # path to zipped data file
if (!file.exists(ZipFile)) {
download.file(fileUrl, destfile = ZipFile, method ="curl")
unzip(ZipFile, exdir=filePath)
dateDownloaded <- date()
}
raw <- read.csv(paste0(filePath, fileName),colClasses = c("integer","character", "integer"))
echo = TRUE
clean <- na.omit(raw)
dailysteps <-as.data.frame(with(clean, tapply(steps, date, sum)))
s <- split(mydat, mydat$date)
View(raw)
if (!file.exists("data")) {
dir.create("data")
}
filePath <- "./data/"
fileZipName <- "repdata-data-activity.zip"
fileName <- "activity.csv"
fileUrl <- "https://d396qusza40orc.cloudfront.net/repdata%2Fdata%2Factivity.zip"
ZipFile <- paste0(filePath, fileZipName)        # path to zipped data file
if (!file.exists(ZipFile)) {
download.file(fileUrl, destfile = ZipFile, method ="curl")
unzip(ZipFile, exdir=filePath)
dateDownloaded <- date()
}
raw <- read.csv(paste0(filePath, fileName),colClasses = c("integer","character", "integer"))
echo = TRUE
clean <- na.omit(raw)
dailysteps <-as.data.frame(with(clean, tapply(steps, date, sum)))
# s <- split(mydat, mydat$date)
echo = TRUE
clean <- na.omit(raw)
dailysteps <-as.data.frame(with(clean, tapply(steps, date, sum)))
View(dailysteps)
names(dailysteps)
View(clean)
clean$date <- as.factor(clean$date)
dailysteps <-as.data.frame(with(clean, tapply(steps, date, sum)))
dailysteps <-as.data.frame(with(clean, tapply(steps, date, sum)))
View(dailysteps)
x <- rowsums(clean$steps)
x <- rowSums(clean$steps)
x <- rowSums(clean)
?rowSums
?lapply
x <- with(clean, tapply(steps, date, sum))
str(x)
hist(x)
summary(x)
mean(x)
sum(x)
View(clean)
as.data.frame(x)
x <- rowSums(clean)
if (!file.exists("data")) {
dir.create("data")
}
filePath <- "./data/"
fileZipName <- "repdata-data-activity.zip"
fileName <- "activity.csv"
fileUrl <- "https://d396qusza40orc.cloudfront.net/repdata%2Fdata%2Factivity.zip"
ZipFile <- paste0(filePath, fileZipName)        # path to zipped data file
if (!file.exists(ZipFile)) {
download.file(fileUrl, destfile = ZipFile, method ="curl")
unzip(ZipFile, exdir=filePath)
dateDownloaded <- date()
}
raw <- read.csv(paste0(filePath, fileName),colClasses = c("integer","character", "integer"))
echo = TRUE
clean <- na.omit(raw)
clean$date <- as.factor(clean$date)
dailysteps <- with(clean, tapply(steps, date, sum))
mean(dailysteps)
hist(dailysteps)
#dailysteps <-as.data.frame(with(clean, tapply(steps, date, sum)))
# s <- split(mydat, mydat$date)
mean(dailysteps)
?hist
print("Mean Number of Daily Steps = ")
print("Mean Number of Daily Steps = " mean(dailysteps))
print("Mean Number of Daily Steps = ", mean(dailysteps))
?print
setwd("~/CourseraHW/RepData_PeerAssessment1")
if (!file.exists("data")) {
dir.create("data")
}
filePath <- "./data/"
fileZipName <- "repdata-data-activity.zip"
fileName <- "activity.csv"
fileUrl <- "https://d396qusza40orc.cloudfront.net/repdata%2Fdata%2Factivity.zip"
ZipFile <- paste0(filePath, fileZipName)        # path to zipped data file
if (!file.exists(ZipFile)) {
download.file(fileUrl, destfile = ZipFile, method ="curl")
unzip(ZipFile, exdir=filePath)
dateDownloaded <- date()
}
raw <- read.csv(paste0(filePath, fileName),colClasses = c("integer", "character", "integer"))
clean <- na.omit(raw)
clean$date <- as.factor(clean$date)
dailysteps <- with(clean, tapply(steps, date, sum))
myplot <- hist(dailysteps, xlab='steps', main='Histogram of Daily Steps')
echo = TRUE
cat("Mean Number of Daily Steps = ", mean(dailysteps))
cat("Median Number of Daily Step =", median(dailysteps))
?plot
?avg
?average
if (!file.exists("data")) {
dir.create("data")
}
filePath <- "./data/"
fileZipName <- "repdata-data-activity.zip"
fileName <- "activity.csv"
fileUrl <- "https://d396qusza40orc.cloudfront.net/repdata%2Fdata%2Factivity.zip"
ZipFile <- paste0(filePath, fileZipName)        # path to zipped data file
if (!file.exists(ZipFile)) {
download.file(fileUrl, destfile = ZipFile, method ="curl")
unzip(ZipFile, exdir=filePath)
dateDownloaded <- date()
}
raw <- read.csv(paste0(filePath, fileName),colClasses = c("integer", "character", "integer"))
clean <- na.omit(raw)
clean$date <- as.factor(clean$date)
clean$interval <- as.factor(clean$interval)
dailysteps <- with(clean, tapply(steps, date, sum))
myhist <- hist(dailysteps, xlab='steps', main='Histogram of Daily Steps')
echo = TRUE
cat("Mean Number of Daily Steps = ", mean(dailysteps))
cat("Median Number of Daily Step =", median(dailysteps))
intervalsteps <- with(clean, tapply(steps, interval, mean))
my <- plot(intervalsteps, type="l", xlab='5-minute interval', ylab='Number of Steps',
main='Average Number of Steps')
View(clean)
intervalsteps
str(intervalsteps)
y <- as.data.frame(intervalsteps)
View(y)
plot(y$row.names, y$intervalsteps)
?plot
z <- intervalsteps$V1
plot(intervalsteps, clean$interval , type="l")
plot(intervalsteps, interval, type="l")
plot(with(clean, tapply(steps, interval, mean), interval)
)
plot(with(clean, tapply(steps, interval, mean), interval), type="l")
plot(with(clean, tapply(steps, interval, mean), as.factor(interval), type="l")
)
summary(intervalsteps)
str(intervalsteps)
cat(intervalsteps[1])
dimnames(intervalsteps)
?dimnames
xlab='5-minute interval', ylab='Number of Steps',
View(y)
View(y)
plot(y$row.names,y$intervalsteps)
plot(y$V1,y$intervalsteps)
plot(y[,1],y[,2])
y[1,2]
y[1,1]
y[1,row.names]
